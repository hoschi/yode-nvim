describe('detach and reload -', function()
    pending('detach buffer by reload disk changes with `e!`')
    pending('external update on current buffer')
    pending(
        'external update on not visible buffer. Make add empty line and test height is correct after reset.'
    )
    pending('external update on multiple buffers')
end)
